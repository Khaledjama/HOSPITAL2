@model IEnumerable<HOSPITAL2.Models.Doctor>
    

@{
    ViewBag.Title = "Index2";
}

<h2>مواعيـــــــــــــــــــــــــد العمل</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayName("الدوكتـــــور")
        </th>
        <th>
            @Html.DisplayName("موعد  الانتهاء")
        </th>
        <th>
            @Html.DisplayName("موعد البدأ")
        </th>
        <th>
            @Html.DisplayName("التخصص")
        </th>
        <th></th>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EndAt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.startat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Department.Name)
        </td>
    </tr>
}

</table>
